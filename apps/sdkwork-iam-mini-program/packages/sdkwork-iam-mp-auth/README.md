<!-- SDKWORK-CLIENT-APP-SURFACES-GENERATED: do not edit by hand; regenerate with `node scripts/materialize-client-app-surfaces.mjs`. -->
# @sdkwork/iam-mp-auth

| Field | Value |
| --- | --- |
| Role | `capability` |
| Surface | `app` |
| Capability | `auth` |
| Layer role | `frontend-feature` |
| SDK boundary | `app-api` (authenticated-app-api) |

## Routes

- `app.iam.auth.login` → `/auth/login` (`iam.auth.login.title`)
- `app.iam.auth.register` → `/auth/register` (`iam.auth.register.title`)
- `app.iam.auth.forgot-password` → `/auth/forgot-password` (`iam.auth.forgotPassword.title`)
- `app.iam.auth.oauth-callback` → `/auth/oauth/callback` (`iam.auth.oauthCallback.title`)
- `app.iam.auth.context-selection` → `/auth/login/context` (`iam.auth.contextSelection.title`)

