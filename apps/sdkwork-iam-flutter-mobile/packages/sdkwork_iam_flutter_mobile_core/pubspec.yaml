name: sdkwork_iam_flutter_mobile_core

description: Cross-surface IAM runtime helpers for sdkwork-iam Flutter mobile roots.

version: 0.1.0

publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
